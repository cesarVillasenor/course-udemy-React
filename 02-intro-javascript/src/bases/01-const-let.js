// Vars and constantes

    const firstName = 'Cesar'; 
    const lastName = 'Villasenor';

    let  diceValue =  5;
    diceValue = 4 

    console.log( firstName, lastName, diceValue);
    
    // var  Should not be used...
    if ( true ){
        const firsName = 'peter';
        let diceValue = 6;

        console.log( firsName, diceValue );
    }

    console.log(diceValue);